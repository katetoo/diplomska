<%= render 'shared/page_title', title: "Сите корисници" %>

<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <%= will_paginate %>
  </div>
</div>

<%= will_paginate %>
<% @users.each do |user| %>
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <p>
        <h4><%= user.ime_korisnik %> <%= user.prezime_korisnik %></h4>
        <%= link_to "Види повеќе", user_path(user) %>
      </p>
    </div>
  </div>
<% end %>
<%= will_paginate %>